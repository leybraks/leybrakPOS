# Copia este archivo a `keystore.properties` (mismo directorio) y rellena los
# valores reales. NUNCA subas keystore.properties ni el .keystore al repo.
#
# storeFile es relativo a android/app/
storeFile=leybrak-release.keystore
storePassword=CAMBIA_ESTO
keyAlias=leybrak
keyPassword=CAMBIA_ESTO
