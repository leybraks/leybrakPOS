// ⚠️ IMPORTANTE: este número DEBE coincidir con `versionCode` en
// android/app/build.gradle. Cada vez que saques una versión nueva, sube
// AMBOS (este y el versionCode del build.gradle) en +1.
export const APP_VERSION_CODE = 23;

// Solo para mostrar en pantalla (debe coincidir con versionName).
export const APP_VERSION_NAME = '1.0.22';
